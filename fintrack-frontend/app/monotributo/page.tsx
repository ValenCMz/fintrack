import PlaceholderPage from "@/app/pages/PlaceholderPage";
import { FileText } from "lucide-react";

export default function MonotributoPage() {
  return (
    <PlaceholderPage
      title="Monotributo"
      description="Lleva el control de tus pagos de monotributo y obligaciones fiscales."
      icon={FileText}
    />
  );
}
